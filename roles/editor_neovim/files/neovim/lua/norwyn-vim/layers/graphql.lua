-- GraphQL: APIs for the 21st Century

local M = {}

function M.plugins(opts)
  return { 
    ["jparise/vim-graphql"] = { },
  }
end

function M.pre_config(opts)
end

function M.config(opts)
end

function M.post_config(opts)
end

return M
